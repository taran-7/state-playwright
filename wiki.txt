Inside that directory, you can run several commands:

  npx playwright test
    Runs the end-to-end tests.

  npx playwright test --project=chromium
    Runs the tests only on Desktop Chrome.

  npx playwright test example
    Runs the tests in a specific file.

  npx playwright test --debug
    Runs the tests in debug mode.

  npx playwright codegen
    Auto generate tests with Codegen.

We suggest that you begin by typing:

    npx playwright test

Форматирование кода ctrl + alt + L
Форматирование кода shift + alt + L
Дублирование строки ctrl + D
USERNAME='XXXXXXXXXXXXXX' PASSWORD='XXXXXXXXXXX' npx playwright test --headed

npx playwright test checkSite.spec.js --headed
npx playwright test loginFormHotelConnect.js --debug

Bush

USERNAME=a.tak@b PASSWORD=1260 npx playwright test --headed

USERNAME='a.tara@bit' PASSWORD='1260' npx playwright test checkSite.spec.js --headed

USERNAME='a.tk@bi' PASSWORD='60' npx playwright test checkSite.spec.js --debug

BASE_URL="https://"
USERNAME="a.ta@bi"
PASSWORD="1260C0tiki1260"


